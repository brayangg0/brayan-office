{
  "name": "brayan-office-crm",
  "version": "1.0.0",
  "description": "Brayan Office - CRM WhatsApp",
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd ../backend && npm install && npx prisma generate && npm run build",
    "start": "cd backend && npx prisma db push && node dist/index.js"
  }
}
